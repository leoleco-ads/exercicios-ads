//Some todos os numeros multiplos de 5 de 1 até 50

#include <stdio.h>

int main () {
    int soma = 0;
    int num = 1;

    while (num<=50) {
        if (num%5==0) {          
        soma = soma + num;
        }
        num++;
    }
    printf("%d\n", soma);
    return 0;
}
