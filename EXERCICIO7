//Faça a soma de 1 até 20, somente os pares.

#include <stdio.h>

int soma = 0;
int i = 1;

int main () {
    while(i<=20) {
        if (i % 2 == 0) {
        soma = soma + i;
        }
        i++;
        }
        printf("%d\n", soma);
    return 0;
    
}
